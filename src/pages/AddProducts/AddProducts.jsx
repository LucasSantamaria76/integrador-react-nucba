import { MainContainer } from '../../components/common';
import { WrapperForm } from './AddProducts.styles';

const AddProducts = () => {
  return (
    <MainContainer>
      <WrapperForm></WrapperForm>
    </MainContainer>
  );
};

export default AddProducts;
